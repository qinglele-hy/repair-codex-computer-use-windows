# Codex Skill Files

These files mirror the local Codex skill:

```text
repair-codex-computer-use-windows
```

To install manually, copy this directory to:

```text
%CODEX_HOME%\skills\repair-codex-computer-use-windows
```

Keep `SKILL.md`, `openai.yaml`, and `failure-signatures.md` together.
